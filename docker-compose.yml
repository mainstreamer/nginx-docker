version: "3.8"

services:
  nginx:
    container_name: "nginx-router"
    env_file:
      - .env
    build:
      args:
        SERVER_IP: ${SERVER_IP}
      context: .
      dockerfile: .docker/nginx/Dockerfile
    restart: always
    volumes:
      - "capitals-app:/var/www/html/public"
    networks:
      - backend-flags
    ports:
      - "80:80"
      - "443:443"
networks:
  backend-flags:
    external: true
volumes:
  capitals-app:
    external: true